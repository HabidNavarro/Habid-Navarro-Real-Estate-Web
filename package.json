{
  "name": "habid-navarro-web",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "astro build && node --test tests/**/*.mjs"
  },
  "dependencies": {
    "astro": "^5",
    "@astrojs/sitemap": "^3"
  }
}
